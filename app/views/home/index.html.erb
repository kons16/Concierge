<h1>トップページ</h1>

<% if user_signed_in? %>
  <% if @announce != "" %>
    <%= @announce %>
    (<%= link_to @origin_user.name, user_path(@origin_user.id) %>)
    <%= link_to "チャットルームへ" %>
  <% end %>
<% else %>
<% end %>
